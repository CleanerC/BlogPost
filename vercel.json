{
    "buildCommand": "cd frontend && npm install && npm run build && cd ../server && npm install && cd .. && npx prisma generate",
    "outputDirectory": "frontend/dist",
    "rewrites": [
      { "source": "/api/(.*)", "destination": "/api" },
      { "source": "/(.*)", "destination": "/index.html" }
    ]
  }